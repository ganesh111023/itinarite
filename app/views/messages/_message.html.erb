<div class="row chat-content <%= (message.user.try(:name) == current_user.name) ? "left-al" : "right-al" %>" >
	<li>
	  <div class="chat-img">
	    <img src="http://placehold.it/50x50" />
	  </div>
	  <div class="chatbox-message-content">

	    <p><%= message.body %></p>
	    <%= link_to "delete", conversation_message_path(conversation_id: conversation_id, id: message.id ), method: :delete,remote: true  %>
	 <!--  <time datetime="<%= message.created_at %>" title="<%= message.created_at.strftime("%d %b  %Y at %I:%M%p") %>">
	      <%#= message_interlocutor(message).name %> • <%= message.created_at.strftime("%H:%M %p") %>
	    </time> -->
	  </div>
	</li>
</div>